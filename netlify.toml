# Netlify deploys this repository as-is: there's no build step, and the site is served from the repo root (index.html).
[build]
  publish = "."

# The app used to be a single file, blockout.html — send old links to the new page.
[[redirects]]
  from = "/blockout.html"
  to = "/"
  status = 301
